const NoDataMsg = ({message}) => {
    return ( 
        <div className="text-center dark:text-white w-full p-4 rounded-full my-4 bg-white">
            <p>{message}</p>
        </div>
     );
}
 
export default NoDataMsg;